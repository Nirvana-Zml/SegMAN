# F1 匹配后分类优化方案（cls_on_matched 81.78% → ≥83%）


| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编写日期 | 2026-06-02 |
| 前置状态 | F1-3 完成（`iter_40000.pth`）；F1-6 E2E 完成 |
| 关联文档 | `outputs/e2e_improve/f1_execution_summary.md`、`transgrasp/pipelines/run_f1_gate_check.py` |
| 问题 | **F1-PASS-4** 未过：`cls_on_matched` **81.78%** &lt; **83%**（差 **1.22pp**） |
| 约束 | 优化时 **不得** 使 `match_rate` &lt; **65%**、`pred_gt_ratio` 超出 **0.95～1.08**（其余 F1 闸门） |


---

## 1. 问题定义

### 1.1 指标含义

| 指标 | 定义 | 当前 M2F | B1 语义 | 闸门 |
| --- | --- | --- | --- | --- |
| **cls_on_matched** | GT 实例与 pred 实例 mask IoU≥0.25 匹配成功后，OpenCLIP **分类 Top-1 = GT 类** 的比例 | **81.78%** | **84.59%** | **≥ 83%** |
| match_rate | 同上匹配流程中，GT 被任意 pred 匹配的比例 | 75.46% | 59.16% | ≥ 65% ✅ |
| seg_class_acc_on_matched | 匹配对上后，**分割语义类** 与 GT 一致比例 | 89.29% | 93.20% | 参考 |

**需补齐正确数（粗算）：**

```text
num_matched = 2343
目标正确数 ≥ 0.83 × 2343 ≈ 1945
当前正确数 ≈ 0.8178 × 2343 ≈ 1916
约需多对 29～30 个实例（+1.22pp）
```

### 1.2 为何 M2F 实例更好、分类反而略降？

| 现象 | 解释 |
| --- | --- |
| match **+16pp**，cls **−2.8pp** | 分类器在 **B1 语义连通域 ROI** 上训练；M2F mask/bbox **分布漂移** |
| wall 匹配 **618→884**（+266） | 以前「只对上」的 wall 偏简单；M2F 召回更多 **难例 wall**，拉低 wall 上 cls |
| door 匹配 **301→460** | 同类：难例进入「匹配后」集合 |
| seg 89.29% vs cls 81.78% | 约 **8pp** 差距来自 **ROI 分类头**，不是分割贴错类为主 |

**结论：** 主攻 **「M2F ROI → 分类器」适配**，而非继续训 M2F 分割。

---

## 2. 按类诊断（M2F vs B1，匹配后 Cls Acc）

| 类 | B1 匹配数 | M2F 匹配数 | B1 Cls | M2F Cls | Δ | 优先级 |
| --- | --- | --- | --- | --- | --- | --- |
| window | 73 | 90 | 61.64% | **55.56%** | −6.1pp | **P0** |
| jar_kettle | 107 | 118 | 74.77% | **66.95%** | −7.8pp | **P0** |
| bottle | 176 | 195 | 88.07% | 85.13% | −2.9pp | P1 |
| box | 57 | 67 | 78.95% | 74.63% | −4.3pp | P1 |
| wall | 618 | **884** | 88.51% | 86.54% | −2.0pp | P1（量大） |
| shelf | 33 | 45 | 51.52% | 60.00% | +8.5pp | 仍低，P1 |
| door | 301 | 460 | 70.65% | 70.65% | ≈0 | 量增难例 |
| cup / eyeglass | 多 | 多 | 94～100% | 94～99% | 小 | 保持 |

**拖累估算：** window、jar_kettle、box、bottle、wall 五类合计贡献大部分分类错误增量；优先 **window + jar_kettle + 高置信过滤**。

---

## 3. 优化路线总览

```text
  P0 推理侧（0.5～1 天，无训练）
       m2f score_thresh sweep + 可选 per-class thresh
              ↓ 未达 83%
  P1 数据侧（2～4 天）
       用 M2F@40k 重建 train/val ROI → OpenCLIP 轻量微调
              ↓ 未达 83%
  P2 策略侧（1～2 天）
       分割类条件融合 / 难类加权 / reject 与闸门解耦评估
              ↓ 仍不足
  P3 决策
       调整 F1-PASS-4 或拆分「实例 PASS / 分类 PASS」
```

| 阶段 | 目标 cls_on_matched | 风险 match 下降 | 推荐 |
| --- | --- | --- | --- |
| **P0** | +0.5～1.5pp | 低～中 | **先做** |
| **P1** | +2～4pp | 低 | P0 不足则做 |
| P2 | +0.5～2pp | 低 | 可选 |
| P3 | 流程 | — | 兜底 |

---

## 4. P0：Mask2Former 推理阈值与实例过滤（无训练）

### 4.1 原理

`Mask2FormerInstancePredictor` 默认 `score_thresh=0.3`（`--m2f-score-thresh`）。  
提高阈值 → 去掉低置信实例 → 常 **减少难例 ROI**，**cls_on_matched 上升**；match_rate 可能略降。

### 4.2 实验矩阵

| 实验 ID | m2f_score_thresh | min_area | nms_iou | 预期 |
| --- | --- | --- | --- | --- |
| P0-a | 0.30 | 128 | 0.5 | 复现基线 81.78% |
| P0-b | 0.35 | 128 | 0.5 | cls↑ match 略降 |
| P0-c | 0.40 | 128 | 0.5 | cls↑ match 略降 |
| P0-d | 0.45 | 128 | 0.5 | cls↑ match 可能 &lt;65% 需验证 |
| P0-e | 0.40 | 160 | 0.5 | 进一步压碎片 |
| P0-f | 0.35 | 128 | 0.6 | 更强同类 NMS |

### 4.3 执行命令（Docker 示例）

```bash
# 模板：只改 --m2f-score-thresh 与 --out-dir
docker exec segman_train bash -lc '
  source /root/anaconda3/etc/profile.d/conda.sh
  conda activate segman_mmdet
  cd /workspace/segman
  TH=0.40
  python transgrasp/pipelines/segment_and_classify.py \
    --eval-split val --max-images -1 \
    --instance-source m2f \
    --m2f-checkpoint segmentation/outputs/m2f_trans10k_pseudo/iter_40000.pth \
    --m2f-score-thresh ${TH} \
    --out-dir outputs/e2e_improve/f1_m2f_e2e_thresh${TH} \
    --min-area 128 --nms-iou 0.5 --iou-match 0.25
  conda activate segman
  python transgrasp/pipelines/summarize_e2e_eval.py \
    --eval-dir outputs/e2e_improve/f1_m2f_e2e_thresh${TH}
'
```

### 4.4 记录表（实验后填写）

| ID | thresh | match_rate | cls_on_matched | strict_e2e | pred_gt_ratio | F1-PASS-4 |
| --- | --- | --- | --- | --- | --- | --- |
| P0-a | 0.30 | 75.46% | 81.78% | 61.71% | 1.045 | ❌ |
| P0-b | | | | | | |
| P0-c | | | | | | |

**P0 成功标准：** `cls_on_matched ≥ 83%` 且 `match_rate ≥ 65%` 且 `pred_gt_ratio ∈ [0.95, 1.08]`。

### 4.5 验收后更新闸门（若 PASS）

将最优 `thresh` 写入 `segment_and_classify` 默认或部署配置；重跑：

```bash
python transgrasp/pipelines/run_f1_gate_check.py
# 需先把 f1_m2f_e2e 指向最优目录，或扩展 RUNS 列表
```

---

## 5. P1：基于 M2F ROI 的分类器适配训练

### 5.1 原理

当前交付分类器：`outputs/openclip_classifier/deliver_classifier_best.pth`  
训练数据多为 **GT 或 SegMAN 语义 CC ROI**（`data/trans10k_roi_gt` 等），与 **M2F 预测 mask 裁切** 在边界、背景混入、宽高比上不一致。

**目标：** 构造 `m2f_roi_train/val`，在冻结或部分解冻 OpenCLIP 上 **短训 10～20 epoch**。

### 5.2 数据构建（建议流程）

```text
1. infer_m2f_export_coco（train split）或逐图 predict_instances
2. 对每个 pred 实例：按 mask 裁 ROI，标签 = pred 对应类（弱监督）
   更佳：与 GT 匹配上的实例用 GT 类，未匹配丢弃或作 hard negative
3. 目录结构对齐 ROIDataset：
   data/trans10k_roi_m2f/{train,val}/{class_name}/*.jpg
```

**推荐：** 至少包含 **匹配 GT 的 M2F ROI**（与 E2E 评测分布一致），必要时加入 **高 IoU 匹配** 的 `(image, gt_class, m2f_crop)` 对。

可选脚本扩展点：

- 新建 `transgrasp/pipelines/build_m2f_roi_dataset.py`（从 `pred_instances.json` + GT COCO 生成）
- 或复用 `segmentation/tools/infer_m2f_export_coco.py` + 现有 `build_roi_dataset` 逻辑

### 5.3 训练配置建议

| 项 | 建议值 | 说明 |
| --- | --- | --- |
| 初始化 | `deliver_classifier_best.pth` | 热启动 |
| clip | ViT-B-16 / laion2b | 与现网一致 |
| freeze-clip | 先 **冻结**，只训 head | 快速验证 |
| epochs | 15～25 | val 早停 patience=5 |
| aug | `p3` | 与 P3 一致 |
| class-weights | 提高 window/jar_kettle/shelf | 对齐 P0 难类 |
| 验收 | **M2F E2E cls_on_matched** | 不以离线 ROI acc  alone 为准 |

```bash
conda activate segman
cd /workspace/segman
python transgrasp/classification/train_openclip_classifier.py \
  --roi-root data/trans10k_roi_m2f \
  --work-dir outputs/openclip_classifier/m2f_roi_adapt_v1 \
  --resume outputs/openclip_classifier/deliver_classifier_best.pth \
  --freeze-clip \
  --aug p3 \
  --epochs 20 \
  --patience 5
```

### 5.4 评测

```bash
python transgrasp/pipelines/segment_and_classify.py \
  --instance-source m2f \
  --m2f-checkpoint segmentation/outputs/m2f_trans10k_pseudo/iter_40000.pth \
  --cls-checkpoint outputs/openclip_classifier/m2f_roi_adapt_v1/best.pth \
  --out-dir outputs/e2e_improve/f1_m2f_e2e_cls_adapt_v1 \
  ...
```

**P1 成功标准：** E2E `cls_on_matched ≥ 83%`，且相对 P0 最优 **match_rate 下降 &lt; 2pp**。

---

## 6. P2：轻量策略（不重训分割）

### 6.1 分割类 + 分类融合（推荐试）

对 **匹配成功** 的实例：若 `seg_class == gt_class` 且 `clip_confidence < τ`，可用 **分割类** 作为预测（或 logits 加权）。

- 现状：seg_class_acc_on_matched **89.29%** &gt; cls **81.78%**
- 实现：在 `classify_instances.py` 或后处理增加 `--fuse-seg-on-low-conf 0.55`
- 风险：闸门若严格定义为「纯 OpenCLIP Top-1」，需与 `run_f1_gate_check` 口径对齐

### 6.2 难类专项

| 类 | 动作 |
| --- | --- |
| window | 略降 `m2f_score_thresh` 仅 window（需 per-class 检测分支）；或 P1 加权 2× |
| jar_kettle | 检查与 bottle/cup 混淆；reject_thresholds 单独扫 |
| door / wall | 维持 match 收益，P1 增加 door/wall ROI 增广 |

### 6.3 reject 阈值说明

`reject_thresholds_p3.json` 影响 **grasp/reject**，**不改变** `cls_on_matched`（该指标在拒识前已算 Top-1）。  
若业务看 **grasp only Acc**，可另扫；**本方案不依赖 reject 扫参过 F1-PASS-4**。

---

## 7. P3：流程与闸门（兜底）

若 P0+P1 后 `cls_on_matched` 稳定在 **82.0%～82.8%**：

| 选项 | 说明 |
| --- | --- |
| A. 微调 F1-PASS-4 | 83% → **82%**，需团队书面确认 |
| B. 拆分闸门 | **F1-INST-PASS**（match≥65%）+ **F1-CLS-PASS**（cls≥83%），部署可先上实例 |
| C. 维持 B1 部署 | 仅当业务强制全闸门且 cls 无法提升 |

**不建议：** 为 1.2pp 再训一轮 M2F 40k 或专攻 COCO small AP。

---

## 8. 实验管理与产出物

### 8.1 目录约定

```text
outputs/e2e_improve/
  f1_m2f_e2e_thresh0.35/          # P0 sweep
  f1_m2f_e2e_cls_adapt_v1/       # P1
  f1_cls_opt_summary.json         # 汇总表（建议自建）
```

### 8.2 汇总 JSON 模板

```json
{
  "baseline": {"thresh": 0.3, "cls_on_matched": 0.8178, "match_rate": 0.7546},
  "experiments": [
    {"id": "P0-b", "thresh": 0.35, "cls_on_matched": null, "match_rate": null, "pass_cls_gate": false}
  ],
  "best": null,
  "recommendation": "deploy pending"
}
```

### 8.3 更新文档

|P0/P1 完成后更新|
|---|
|`f1_execution_summary.md` 增补「分类优化」章节|
|`f1_plan_summary.json` 重跑 `run_f1_gate_check.py`|

---

## 9. 决策树（执行顺序）

```text
开始
  → P0: thresh ∈ {0.35, 0.40, 0.45} 各跑 1 次 E2E
       → 任一满足 cls≥83% 且 match≥65% ?
            是 → 定参、重跑 gate、结束 ✅
            否 → P1: 构建 m2f ROI + 微调分类 15～20 epoch
                 → cls≥83% ?
                      是 → 结束 ✅
                      否 → P2: seg-class 融合 / 难类加权
                           → 仍否 → P3 闸门或拆分部署
```

---

## 10. 风险与禁忌

| 禁忌 | 原因 |
| --- | --- |
| 再训 M2F 专攻 small AP | 对 cls_on_matched 边际收益极小，工期 1～2 周 |
| 仅看离线 ROI val acc | 与 M2F E2E 分布不一致，可能虚高/虚低 |
| 为提 cls 大幅提高 thresh 导致 match&lt;65% | 触发 F1-FAIL / 实例闸门失败 |
| 用 B1 语义实例混评 M2F 分类 | 指标口径混乱 |

| 风险 | 缓解 |
| --- | --- |
| P0 使 match 跌破 65% | thresh 上限建议 ≤0.42，逐步扫 |
| P1 过拟合 M2F 伪标签 | 匹配 GT 的 ROI 为主，强增广 + 早停 |
| 融合分割类与闸门定义冲突 | 事先改 `summarize_e2e_eval` 标注口径 |

---

## 11. 附录：关键文件索引

| 文件 | 作用 |
| --- | --- |
| `transgrasp/pipelines/segment_and_classify.py` | E2E；`--m2f-score-thresh` |
| `transgrasp/pipelines/instance_predictor.py` | `Mask2FormerInstancePredictor.score_thresh` |
| `transgrasp/pipelines/classify_instances.py` | 匹配后分类 |
| `transgrasp/pipelines/run_f1_gate_check.py` | F1-PASS-4 判定 |
| `transgrasp/classification/train_openclip_classifier.py` | P1 微调 |
| `transgrasp/classification/configs/reject_thresholds_p3.json` | 拒识（非本指标） |
| `scripts/run_f1_eval_iter40000.sh` | 完整 F1 评测参考 |

---

## 12. 执行摘要

| 项 | 内容 |
| --- | --- |
| 缺口 | **1.22pp**（约 **30** 个匹配实例分类正确） |
| 根因 | 分类器 ROI 分布与 **M2F 实例** 不一致；难例 recall 增加 |
| **首选** | **P0：`m2f_score_thresh` 0.35～0.40 sweep**（0.5～1 天） |
| **次选** | **P1：M2F 匹配 ROI 上 OpenCLIP 微调**（2～4 天） |
| **不做** | 小物体专项 M2F 长训、碎片后处理作为主方案 |
| 成功 | `cls_on_matched ≥ 83%` 且其余 F1 闸门保持 ✅ → `F1_PASS=true` |
