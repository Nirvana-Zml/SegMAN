# 双轨交付与软著材料清单

| 项 | 内容 |
| --- | --- |
| 版本 | V1.0.0 |
| 日期 | 2026-06-02 |
| 主文档 | `docs/交付/SegMAN_OpenCLIP_E2E_交付路线.md` |
| Manifest | `outputs/e2e_improve/deliver_dual_track_manifest.json` |

---

## 1. 软件正式名称（建议）

- **全称**：TransGrasp 透明物体语义感知与抓取细分类系统 V1.0  
- **简称**：TransGrasp 双轨感知分类系统 V1.0  

（最终以单位知识产权办审定为准。）

---

## 2. 交付 zip 目录树

```text
TransGrasp-V1.0.0/
├── README_交付说明.md
├── LICENSE_第三方组件.txt
├── app/
│   ├── run_semantic_e2e.py
│   └── run_grasp_e2e.py
├── transgrasp/
├── segmentation/
├── scripts/
│   ├── run_deliver_semantic_e2e.sh
│   ├── run_deliver_grasp_e2e.sh
│   ├── setup_f1_mmdet_env.sh
│   └── promote_dual_track_deliver.sh
├── docs/
│   ├── 交付/SegMAN_OpenCLIP_E2E_交付路线.md
│   └── 交付/双轨交付与软著材料清单.md
├── outputs/e2e_improve/
│   └── deliver_dual_track_manifest.json
└── models/   # 见 MODELS_README.md：三个 ckpt 路径与 MD5
```

---

## 3. 说明书章节大纲（软著 / 用户手册共用）

1. 引言：透明物体抓取、11 类、双轨设计原因  
2. 运行环境：OS、GPU、Python、segman / segman_mmdet  
3. 软件结构：五模块（语义分割、实例检测、细分类、拒识、评测）  
4. **模式 A 流程图**：RGB → SegMAN → CC → OpenCLIP → 输出  
5. **模式 B 流程图**：RGB → M2F → OpenCLIP → 输出  
6. 输入输出：`--image`、JSON 字段（class_name、confidence、action、bbox）  
7. 安装与运行：`run_semantic_e2e.py` / `run_grasp_e2e.py` 示例  
8. 指标说明：双轨对比表（§4 交付路线文档）  
9. 第三方组件：PyTorch、OpenCLIP、mmdet、mmseg  

---

## 4. 源代码页选文件（前 30 + 后 30 页建议）

**优先自研、代表性强：**

| 序号 | 文件 |
| --- | --- |
| 1 | `app/run_semantic_e2e.py` |
| 2 | `app/run_grasp_e2e.py` |
| 3 | `transgrasp/pipelines/segment_and_classify.py` |
| 4 | `transgrasp/pipelines/seg_model.py` |
| 5 | `transgrasp/pipelines/classify_instances.py` |
| 6 | `transgrasp/pipelines/instance_predictor.py` |
| 7 | `transgrasp/pipelines/roi_extract.py` |
| 8 | `transgrasp/pipelines/roi_postprocess.py` |
| 9 | `transgrasp/classification/train_openclip_classifier.py`（节选） |
| 10 | `scripts/run_deliver_semantic_e2e.sh` / `run_deliver_grasp_e2e.sh` |

页眉：`TransGrasp V1.0.0` + 页码；每页约 50 行。

---

## 5. 验收对照表

| 验收项 | 模式 A | 模式 B |
| --- | --- | --- |
| 脚本 | `run_deliver_semantic_e2e.sh` | `run_deliver_grasp_e2e.sh` |
| 参考指标目录 | `f1_b1_ref` | `f1_m2f_e2e` |
| match_rate | ~59% | ~75% |
| 生产抓取默认 | 否 | **是** |

---

## 6. 软著申请流程备忘

1. 单位知识产权办领取申请表模板  
2. 确定著作权人（学校/企业）  
3. 准备源代码 60 页 + 说明书  
4. 在线提交 / 缴费 / 等待证书  

本清单不构成法律意见；流程以中国版权保护中心当年规定为准。
