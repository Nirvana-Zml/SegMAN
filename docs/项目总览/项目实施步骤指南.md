# 透明物体 SegMAN 项目实施步骤指南

> 本文是《透明物体分割_SegMAN优化设计说明书.md》的 **可执行路线图**。  
> 按顺序完成每一步；打勾项通过后再进入下一步。  
> 项目根目录记为：`SegMAN/`（即本文件所在目录）。

---

## 总览：你要走完的三条路

| 路线 | 目标 | 预计周期（参考） |
|------|------|------------------|
| **路线 A** | 环境与基线 SegMAN 跑通 | 1～3 天 |
| **路线 B** | Trans10K-v2 + LASS/MMSCopE：**透明物体分割**（mask） | 2～4 周 |
| **路线 C** | 分割推理 + PyBullet **抓取仿真** + 交互 UI | 2～4 周 |

```text
路线 A ──► 路线 B ──► 路线 C
 环境     SegMAN 分割(mask)   infer_segman + ASGrasp + PyBullet 仿真/UI
```

路线 C **不单独训练分类模型**；物体类别可由 SegMAN 多类 mask 的 dominant label、用户点选后在界面选择，或 `grasp_class_prior.yaml` 默认参数决定。

---

## 路线 A：环境与基线（必须先完成）


### 步骤 A4：下载 ImageNet 预训练编码器

1. 打开 README 中 Google Drive 链接。  
2. 下载 `SegMAN_Encoder_b.pth.tar`（或你计划用的 t/s/b/l）。  
3. 放到：`SegMAN/pretrained/SegMAN_Encoder_b.pth.tar`。

**验收**：文件存在且体积正常（非几 KB 空文件）。

---

### 步骤 A5：跑通基线 SegMAN 训练/测试命令（可选但强烈建议）

在 **未改网络** 前，用官方 ADE 配置确认环境：

```powershell
cd segmentation
# 仅打印配置（不训练）
python tools\print_config.py local_configs\segman\base\segman_b_ade.py
```

若有 ADE 数据，可试 1 个 epoch；**没有 ADE 也可跳过**，路线 B 直接用 Trans10K。

**验收**：`print_config.py` 能打印完整 config，无 import 错误。

---

### 步骤 A6：熟悉核心代码位置（阅读 30～60 分钟）

| 文件 | 看什么 |
|------|--------|
| `segmentation/mmseg/models/backbones/segman_encoder.py` | `Attention`、`VSSM`、`SegMANEncoder` |
| `segmentation/mmseg/models/decode_heads/segman_decoder.py` | `forward_mlp_decoder`、`forward_winssm` |
| `segmentation/local_configs/segman/base/segman_b_ade.py` | backbone / decode_head 配置写法 |

**验收**：能口头说出「编码器 4 个 stage 输出 4 尺度特征，解码器再融合」。

---

## 路线 B：数据 + 分割模型（LASS + MMSCopE）

### 步骤 B1：获取 Trans10K-v2

1. 从论文/官方渠道下载 **Trans10K-v2**。  
2. 解压到建议路径：

```text
SegMAN/data/Trans10K-v2/
├── train/image/
├── train/mask/
├── val/image/
├── val/mask/
└── （标注文件：类别、划分列表）
```

3. 统计：`类别数 K`、训练/验证张数、mask 格式（0/255 或 0/1）。

**验收**：随机打开 5 张图，mask 与图像尺寸一致，透明区域标注合理。

---

### 步骤 B2：编写 MMSeg 数据转换脚本

**新建文件**：`segmentation/tools/convert_datasets/trans10k.py`

**要做的事**：

1. 把 `image/`、`mask/` 转成 MMSeg 目录结构：

```text
data/trans10k/
├── images/train/
├── images/val/
├── annotations/train/
└── annotations/val/
```

2. 生成 `train.txt` / `val.txt`（每行一个不带后缀的文件名）。  
3. mask 像素值映射为 `0=背景, 1=透明`（或多类 `0..K-1`）。

**新建数据集配置**：`segmentation/local_configs/_base_/datasets/trans10k.py`

```python
# 示例字段，按实际路径改
dataset_type = 'CustomDataset'
data_root = 'data/trans10k/'
img_norm_cfg = dict(mean=[123.675, 116.28, 103.53], std=[58.395, 57.12, 57.375], to_rgb=True)
```

**验收**：

```powershell
cd segmentation
python tools\browse_dataset.py local_configs\segman_trans\segman_b_trans10k.py
```

能弹出/保存增强后的样本图。

---

### 步骤 B3：实现数据增强（反射 / 光照 / 边界模糊）

**建议新建**：`segmentation/mmseg/datasets/pipelines/trans10k_aug.py`（或在 config 里组合现有 pipeline）

| 增强 | 实现方式 |
|------|----------|
| 光照 | `PhotoMetricDistortion` |
| 翻转缩放 | `RandomFlip` + `Resize` + `RandomCrop` |
| 边界模糊 | 自定义：对 gt mask 高斯模糊再量化 |
| 反射模拟 | 自定义：裁背景 patch 贴到 mask 内 RGB 区域 |

在 `segman_b_trans10k.py` 的 `train_pipeline` 里引用。

**验收**：`browse_dataset.py` 能看到明显光照/边界变化。

---

### 步骤 B4：先训「基线 SegMAN」在 Trans10K 上（对照组）

> **职责边界**：本步产出 **透明物体分割 mask（及可选多类语义图）**，供路线 C 抓取仿真使用。  
> Trans10K 的 12 类标注用于训练与 mIoU 评测；抓取阶段可用 mask 内主类别、用户指定类别，或二值透明前景 + 默认抓取参数。可选二分类配置见《Trans10K训练快速开始.md》。

**新建配置**：`segmentation/local_configs/segman_trans/segman_b_trans10k.py`

- `backbone.type = 'SegMANEncoder_b'`（先用原版，不加 LASS）  
- `decode_head.type = 'SegMANDecoder'`（原版）  
- `num_classes` = 12（多类分割训练）或 2（二分类，仅背景/透明）  
- `pretrained` 指向 `pretrained/SegMAN_Encoder_b.pth.tar`

**训练**：

```powershell
cd segmentation
python tools\train.py local_configs\segman_trans\segman_b_trans10k.py --work-dir outputs\baseline_trans10k
```

**验收**：

- `outputs/baseline_trans10k`（或 `outputs/trans10k_segman_b`）有 `latest.pth`，loss 下降；  
- 验证集 **mIoU** 合理；  
- 目视 **mask 边界** 可接受（这是后续抓取仿真的前置条件）。

**训练完成后的评估、可视化、续训与 Docker 说明**：见根目录 **《Trans10K训练快速开始.md》**（文首「训练目的」+ 第 6 步）。

---

### 步骤 B5：实现编码器 LASS（LTAB + RSM）

> **执行清单（基线 mIoU 80.71%、弱类 window/shelf/box、分阶段命令）**：见根目录 **《路线B_LASS_MMSCopE_实施清单.md》**。  
> LTAB/RSM 模块骨架：`segmentation/mmseg/models/modules/ltab.py`、`rsm.py`。

**按设计说明书 §4 实现，建议文件**：

```text
segmentation/mmseg/models/modules/
├── ltab.py              # 低纹理注意力
├── rsm.py               # 反射抑制 + BgHead
segmentation/mmseg/models/backbones/
└── segman_encoder_lass.py   # AttentionLASS, SegMANEncoderLASS
```

**实现顺序（推荐）**：

1. **LTAB**：纹理图 → `W_lt` → 乘到 `VSSM` 输出。  
2. **RSM**：训练用 GT 背景 mask；`v → 抑制 → +δv → VSSM`。  
3. **AttentionLASS**：替换原 `Attention` 中非 global 分支。  
4. **SegMANEncoderLASS**：注册到 `BACKBONES`，`__init__.py` 导出。

**注册**（`segmentation/mmseg/models/backbones/__init__.py`）：

```python
from .segman_encoder_lass import SegMANEncoderLASS
```

**验收**：

```powershell
python -c "
from mmseg.models import build_backbone
import torch
cfg = dict(type='SegMANEncoderLASS', pretrained=None, ...)
m = build_backbone(cfg)
x = torch.randn(1,3,512,512)
print([o.shape for o in m(x)])
"
```

---

### 步骤 B6：实现解码器 MMSCopE（BPM + MSBEC）

**建议文件**：

```text
segmentation/mmseg/models/modules/mmscope.py
segmentation/mmseg/models/decode_heads/segman_decoder_mmscope.py
```

**实现顺序**：

1. `BoundaryProbabilityModule` → `P_bd`, `W_bd`。  
2. 在 `forward_mlp_decoder` 后对 `_c2`、`_c` 加权。  
3. `MultiScaleBoundaryEnhance` → `F_ref`，与 `F_sem` 融合。  
4. `SegMANDecoderMMSCopE` 注册到 `HEADS`。

**损失**：训练时增加 `L_bd`（边界 BCE/Dice），在 `train_step` 或 head 的 `losses` 里加一项。

**验收**：前向 + `loss.backward()` 无 shape 错误。

---

### 步骤 B7：训练完整模型 LASS + MMSCopE

**新建配置**：`segman_b_trans10k_lass.py`

```python
backbone=dict(type='SegMANEncoderLASS', lass_cfg=...)
decode_head=dict(type='SegMANDecoderMMSCopE', mmscope_cfg=...)
```

**训练策略（与设计书 §8 一致）**：

| 子阶段 | 做什么 |
|--------|--------|
| B7-1 | 加载 ImageNet 预训练 + baseline 可选；冻结 stage4，训 20～40k iter |
| B7-2 | 解冻全网，小 lr 精调 |
| B7-3 | `bg_mask_mode` 从 `gt` 逐步改为 `gt+pred` |

```powershell
python tools\train.py local_configs\segman_trans\segman_b_trans10k_lass.py --work-dir outputs\lass_mmscope
```

**验收**：

```powershell
python tools\test.py local_configs\segman_trans\segman_b_trans10k_lass.py outputs\lass_mmscope\latest.pth --eval mIoU
```

对比 **B4 基线**：透明类 IoU、边界 F-score 有提升（设计书消融 E3/E5）。

---

### 步骤 B8：封装分割推理 API（给阶段三用）

**新建**：`transgrasp/segmentation/infer_segman.py`（目录可后建，此处先定接口）

```python
def predict(image_bgr: np.ndarray) -> dict:
    """
    返回:
      - mask: (H,W) uint8  0/1
      - boundary: (H,W) float 可选
      - instances: List[dict] 可选，连通域 bbox
    """
```

**验收**：对单张 Trans10K 验证图，mask 与 GT 目视接近；推理时间可接受。

---

## 路线 C：分割推理 + 抓取仿真一体化

> **前提**：路线 B 的分割权重可用（如 `outputs/lass_mmscope/latest.pth` 或基线 `outputs/trans10k_segman_b/iter_80000.pth`）。

### 步骤 C1：创建 transgrasp 工程目录

```powershell
cd d:\SegMAN-main\SegMAN
mkdir transgrasp\segmentation transgrasp\grasping transgrasp\ui transgrasp\configs transgrasp\pipelines
```

**验收**：目录存在，且与下文「文件清单」中 `transgrasp/` 结构一致。

---

### 步骤 C2：安装 PyBullet 与 ASGrasp

```powershell
pip install pybullet
# ASGrasp：克隆官方/论文仓库，按其 README 安装依赖与权重
```

**新建**：

```text
transgrasp/grasping/pybullet_env.py      # 桌面 + 机械臂 + 夹爪
transgrasp/grasping/asgrasp_wrapper.py   # 输入 mask、类别 → 6D pose
transgrasp/configs/grasp_class_prior.yaml
```

**验收**：`pybullet_env.py` 能空载加载 URDF，机械臂关节可动。

---

### 步骤 C3：单次抓取仿真打通

**流程**：

1. Trans10K 一张图 → B8 `infer_segman` → 用户点击/框选实例 mask。  
2. 确定 `class_id`：mask 内语义众数（多类分割）、用户在 UI 下拉选择，或 `grasp_class_prior.yaml` 默认类。  
3. ASGrasp（或启发式规划）生成 `T_grasp`。  
4. PyBullet 执行：approach → close → lift。  
5. 打印成功/失败（是否抬起物体）。

**新建**：`transgrasp/grasping/run_grasp_sim_once.py`

**验收**：至少 1 个场景能完成「夹起并抬起」；失败时有明确日志。

---

### 步骤 C4：抓取参数迭代优化

**新建**：`transgrasp/grasping/optimize_grasp.py`

优化变量示例：`approach_dist, gripper_width, angle_offset, grasp_depth`  
方法：网格搜索或随机搜索 20～50 组，取 PyBullet 评分最高。

**验收**：同一物体上，迭代后成功率 ≥ 固定参数（设计书 G0 vs G1）。

---

### 步骤 C5：用户交互一体化 UI

**推荐**：Gradio（上手快）  
**新建**：`transgrasp/ui/app.py` 或 `transgrasp/pipelines/run_interactive.py`

**界面最小功能**：

1. 上传图片  
2. 显示分割 mask 叠加  
3. 点击选实例  
4. （可选）下拉选择物体类别，用于加载 `grasp_class_prior`  
5. 按钮「仿真抓取」→ 调 C3/C4 → PyBullet GUI 或 GIF/成功标志  

```powershell
pip install gradio
python transgrasp\ui\app.py
```

**验收**：非开发者能完成「选物体 → 跑仿真」全流程。

---

### 步骤 C6：整系统联调与文档收尾

1. 写 `transgrasp/README.md`：环境、分割权重路径、一键启动命令。  
2. 记录分割 checkpoint（如 `outputs/lass_mmscope/latest.pth`）。  
3. 跑 10～20 张验证集 **端到端** 表格：分割 IoU（或与 GT 对比）、仿真抓取成功率。

**验收**：表格可复现；抓取链路可重复运行。

---

## 建议排期（按周）

| 周次 | 任务 |
|------|------|
| 第 1 周 | A1～A6 + B1～B2 |
| 第 2 周 | B3～B4 基线训练 |
| 第 3 周 | B5 LASS 实现与训练 |
| 第 4 周 | B6～B7 MMSCopE + 消融 |
| 第 5 周 | B8 + C1～C2 环境与 PyBullet |
| 第 6 周 | C3～C4 抓取仿真与参数优化 |
| 第 7 周 | C5～C6 UI 与联调 |

---

## 常见问题速查

| 现象 | 处理 |
|------|------|
| `natten` / `selective_scan` import 失败 | 核对 CUDA、PyTorch 版本与 wheel |
| 预训练加载 key mismatch | `load_state_dict(..., strict=False)`，新模块单独初始化 |
| OOM | 减小 `samples_per_gpu`、用 checkpoint、换 `SegMANEncoder_t` |
| 透明类 IoU 不升 | 先查 mask 标注与 `num_classes`；再加 RSM/LTAB 消融 |
| ASGrasp 未装好 | 先用启发式抓取（mask 质心 + 主方向）跑通 C3 |
| PyBullet 物体滑落 | 调 `grasp_class_prior.yaml` + C4 迭代 |

---

## 文件清单速查（做完后应存在）

```text
SegMAN/
├── 透明物体分割_SegMAN优化设计说明书.md    # 设计
├── 项目实施步骤指南.md                      # 本文件
├── pretrained/SegMAN_Encoder_b.pth.tar
├── data/Trans10K-v2/ ...
├── data/trans10k/                          # MMSeg 格式
├── segmentation/
│   ├── tools/convert_datasets/trans10k.py
│   ├── local_configs/segman_trans/
│   │   ├── segman_b_trans10k.py
│   │   └── segman_b_trans10k_lass.py
│   └── mmseg/models/
│       ├── backbones/segman_encoder_lass.py
│       ├── decode_heads/segman_decoder_mmscope.py
│       └── modules/{ltab,rsm,mmscope}.py
├── outputs/baseline_trans10k/
├── outputs/lass_mmscope/
└── transgrasp/
    ├── segmentation/infer_segman.py
    ├── grasping/{pybullet_env,asgrasp_wrapper,optimize_grasp,run_grasp_sim_once}.py
    ├── ui/app.py
    └── configs/{grasp_class_prior.yaml,sim_scene.yaml}
```

---

## 与设计说明书的对照

| 设计说明书章节 | 本指南步骤 |
|----------------|------------|
| §2 数据准备 | B1～B3 |
| §4 LASS | B5、B7 |
| §5 MMSCopE | B6、B7 |
| §7 抓取一体化 | C2～C5 |
| §9 里程碑 M0～M7 | 全文 A～C |

---

**下一步建议**：若你尚未完成环境，从 **步骤 A1** 开始；若环境已就绪且已有 Trans10K，直接从 **步骤 B1** 开始。
